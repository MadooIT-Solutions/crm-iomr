Restricts product creation in Sales to users with Administrator permissions only.

Users with "User: Own Documents Only" or "User: All Documents" access in Sales
can only select existing products in sale order lines and cannot create new products.
