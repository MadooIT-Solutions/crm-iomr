No configuration needed. The module works automatically based on the user's
Sales access rights:

- **Administrator**: can create and edit products
- **User: All Documents**: can only select existing products
- **User: Own Documents Only**: can only select existing products
